<Window x:Class="Galpones.Revit.UI.FamilyPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Galpones — Familias del proyecto"
        Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,10" TextWrapping="Wrap"
                   Text="Elegí el tipo de familia para cada elemento. Para cargar familias del estudio, guardalas como .rfa en la carpeta familias/ junto al proyecto.yaml y usá el botón Importar." />

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Slots}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4">
                        <TextBlock Text="{Binding Titulo}" FontWeight="SemiBold" />
                        <ComboBox Margin="0,2,0,0"
                                  ItemsSource="{Binding Items}"
                                  SelectedItem="{Binding Seleccion, Mode=TwoWay}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Grid.Row="2" x:Name="EstadoImportacion" Margin="0,8,0,0"
                   TextWrapping="Wrap" Foreground="#555" />

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Importar desde familias/…"
                    Padding="10,4" Click="Importar_Click" />
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Cancelar" Padding="10,4" Margin="0,0,8,0" IsCancel="True" />
                <Button x:Name="GenerarButton" Content="Generar" Padding="14,4"
                        IsDefault="True" Click="Generar_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
